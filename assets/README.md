# assets/

Importable media only (textures, audio, fonts, shaders, animation assets).

Godot generates `.import` sidecars here. Keep game data (`.tres` puzzle defs, themes) in `res://resources/` and logic in `res://scripts/`.
