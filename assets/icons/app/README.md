# App icons

| File | Role |
| --- | --- |
| `icon_1024.png` | Project / launcher placeholder (`project.godot` → `application/config/icon`) |

## Before store builds

1. Replace `icon_1024.png` with final brand art (1024×1024 PNG, no transparency required for desktop; keep alpha for mobile adaptive where needed).
2. Export presets: assign the same (or platform-specific) assets under each preset’s icon fields — see [EXPORTS.md](../../../docs/EXPORTS.md).
3. Optional derivatives: `icon_512.png`, Android adaptive foreground/background, iOS AppIcon set, PWA 192/512 — document paths here when added.

Placeholder is neon-purple glass mark (not final store art).
