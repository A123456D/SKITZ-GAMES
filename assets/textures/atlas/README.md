# Atlas placeholders — shared white / glow for particle batching.
# Runtime: SharedAtlas generates ImageTextures if these files are missing.
# Bake PNGs: godot --headless -s res://tools/developer/pack_shared_atlas.gd
