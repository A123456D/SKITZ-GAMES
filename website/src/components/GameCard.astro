---
interface Props {
	href: string;
	title: string;
	summary: string;
	cover: string;
	genre?: string;
}

const { href, title, summary, cover, genre } = Astro.props;
---

<a class="game-link" href={href}>
	<article class="game-item">
		<div class="game-item__media">
			<img src={cover} alt="" width="640" height="640" loading="lazy" />
		</div>
		<div class="game-item__body">
			{genre && <p class="eyebrow">{genre}</p>}
			<h2>{title}</h2>
			<p class="muted">{summary}</p>
			<span class="cta">view game →</span>
		</div>
	</article>
</a>

<style>
	.game-link {
		display: block;
		height: 100%;
		text-decoration: none;
		color: inherit;
	}

	.game-item {
		display: grid;
		gap: 0.9rem;
		height: 100%;
		padding: 0.35rem 0.15rem 0.5rem;
		transition: transform 180ms var(--ease);
	}

	.game-link:hover .game-item {
		transform: translateY(-5px);
	}

	.game-item__media {
		width: 100%;
		max-width: 280px;
		margin-inline: auto;
		background: #fff;
		padding: 0.45rem;
		box-shadow: 5px 6px 0 var(--skitz-red);
		clip-path: polygon(2% 4%, 96% 0%, 100% 94%, 4% 100%);
		transform: rotate(-1.2deg);
	}

	.game-link:nth-child(even) .game-item__media {
		box-shadow: 5px 6px 0 var(--skitz-blue);
		transform: rotate(1deg);
	}

	.game-link:nth-child(3n) .game-item__media {
		box-shadow: 5px 6px 0 var(--skitz-green);
		transform: rotate(-0.6deg);
	}

	.game-item__media img {
		width: 100%;
		height: auto;
		display: block;
		aspect-ratio: 1;
		object-fit: cover;
	}

	.game-item__body {
		text-align: center;
	}

	.game-item__body h2 {
		font-size: clamp(1.55rem, 3.2vw, 2rem);
		margin-bottom: 0.35rem;
	}

	.game-item__body .muted {
		display: -webkit-box;
		-webkit-line-clamp: 3;
		-webkit-box-orient: vertical;
		overflow: hidden;
		margin-bottom: 0.55rem;
		font-size: 0.95rem;
	}

	.cta {
		font-family: var(--font-type);
		color: var(--skitz-blue);
	}

	.game-link:hover .cta {
		color: var(--skitz-red);
	}
</style>
