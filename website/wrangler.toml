name = "skitz-games"
compatibility_date = "2026-08-01"
pages_build_output_dir = "dist"

# Dashboard or:
#   wrangler kv namespace create BOARD
# then bind BOARD to this project so /api/scores and /api/progress persist.
# [[kv_namespaces]]
# binding = "BOARD"
# id = "YOUR_KV_ID"
