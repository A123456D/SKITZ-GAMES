# scenes/

Packed scenes (`.tscn`) — presentation and composition roots.

Compose reusable components rather than deep inheritance. Level *data* belongs in `res://resources/puzzles/`; scenes wire presentation + scripts.
