# addons/

Godot plugins live here (`res://addons/`). Each plugin needs its own folder with a `plugin.cfg`.

Enable/disable via **Project → Project Settings → Plugins**.

Do not put game scenes or shipping content here — only EditorPlugins and intentional runtime addons.
