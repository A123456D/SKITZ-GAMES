# resources/

Runtime Godot `Resource` data (`.tres` / `.res`) — puzzle definitions, themes, localization, configs, save schemas.

Prefer Resource-driven puzzle content so packs expand without new code. No presentation nodes and no gameplay scripts here.
