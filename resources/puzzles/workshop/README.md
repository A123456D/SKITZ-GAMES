# Workshop puzzles

Community / authored levels in the `shiftr_workshop_puzzle` envelope (schema v1).

- Preferred extension: `.shiftr.json`
- Alternate: `.shiftrpz` (same JSON)
- See `docs/LEVEL_EDITOR.md` for fields and migration notes.

Example: `example_align_3x3.shiftr.json`
