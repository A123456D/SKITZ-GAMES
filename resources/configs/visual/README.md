# resources/configs/visual/

Design tokens (`DesignTokens`) and quality tiers (`VisualQualityConfig`).

- `default_design_tokens.tres` — palette, spacing, glow, motion
- `quality_high.tres` / `quality_med.tres` / `quality_low.tres`
