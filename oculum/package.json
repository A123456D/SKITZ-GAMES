{
  "name": "oculum",
  "private": true,
  "version": "0.1.0-beta",
  "description": "OCULUM beta — Veil. Witness. Ascend.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "icons": "node scripts/make-icons.mjs",
    "sfx:generate": "node scripts/generate-sfx.mjs",
    "punch-ui": "node scripts/punch-ui-alpha.mjs",
    "ship": "npm run icons && npm run build && node scripts/copy-to-site.mjs"
  },
  "devDependencies": {
    "sharp": "^0.35.3",
    "typescript": "^5.8.2",
    "vite": "^6.2.0",
    "vitest": "^3.0.8"
  }
}
